import { State } from "./State";
export declare class ReplayableState<T, X> extends State<T, X> {
    private readonly replay$;
    constructor(input: State<T, X>);
    replay(): void;
}
export declare function replayable<T, X, S extends State<T, X>>(state: State<T, X>): ReplayableState<T, X>;
