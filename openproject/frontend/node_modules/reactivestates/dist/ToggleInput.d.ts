import { State } from "./State";
export declare class ToggleInput extends State<boolean, undefined> {
    private trigger$;
    private state;
    constructor(initialState: boolean);
    toggle(): void;
}
export declare function toggleInput(state?: boolean): ToggleInput;
