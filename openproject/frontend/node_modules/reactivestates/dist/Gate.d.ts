import { State } from "./State";
export declare class Gate<T, X> extends State<T, X | undefined> {
    private readonly trigger$;
    private readonly input$;
    readonly passOne: () => void;
    constructor(input: State<T, X>);
}
export declare function gateFor<T, X>(input: State<T, X>): Gate<T, X>;
