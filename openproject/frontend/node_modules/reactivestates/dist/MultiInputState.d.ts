import { Observable } from "rxjs";
import { InputState } from "./InputState";
import { State } from "./State";
export declare type Cache<T> = {
    [key: string]: InputState<T>;
};
export declare class MultiInputState<T> extends State<Cache<T>, undefined> {
    private readonly cache$;
    private readonly change$;
    private readonly remove$;
    constructor();
    clear(): this;
    get(id: string): InputState<T>;
    remove(id: string): InputState<T> | undefined;
    observeChange(): Observable<[string, T | undefined, InputState<T>]>;
    observeRemove(): Observable<string>;
}
export declare function multiInput<T>(): MultiInputState<T>;
